
require draw
require time

draw.background(#fff)

width = env(:size).x
height = env(:size).y

steps = 500
points = for i in 0..steps
  x = linear_map(i [0 steps] [0 width])
  y = noise(time.elapsed() + x*0.01) * height
  [x y]

draw.line_list(#000 points 2.0)
